spring:
  application:
    name: waa-sep-2022-aop
  datasource:
    password: zedM1
    url: jdbc:postgresql://localhost:5432/aop
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 8080